# API routes - handle API calls first
/api/*  /.netlify/functions/api/:splat  200

# SPA fallback - everything else goes to index.html for client-side routing
# Assets will be served directly from the dist folder
/*  /index.html  200